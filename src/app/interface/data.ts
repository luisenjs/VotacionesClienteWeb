export interface Lista {
    id: string;
    lista: string;
    candidato: string;
}

export interface Organizacion {
    id: string;
    lista: string;
    organizacion: string
}

export interface Consulta {
    id: string;
    nombre: string;
    pregunta: string;
}